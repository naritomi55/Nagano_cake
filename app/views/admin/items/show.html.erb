<h1>商品詳細</h1>

  <%= image_tag @item.image, size: "200x200" %>

  <span>商品名</span>
    <%= @item.name %></br>

  <span>商品説明</span>
    <%= @item.introduction %></br>

  <span>ジャンル</span>
    <%= @item.genre.name %></br>

  <span>税込価格</span>
    <%= @item.add_tax_price %>(<%= @item.price %>)円</br>

  <span>販売ステータス</span>
    <%= @item.is_active == true ? "販売中" : "販売停止" %></br>

  <%= link_to '編集する', edit_admin_item_path %>
