<div class="container">
  <h2>ログイン</h2>
  <p>会員の方はこちらからログイン</p>
  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="field">
    <%= f.label "メールアドレス" %>
    <%= f.email_field :email, autofocus: true, autocomplete: "email", class:"offset-md-1" %>
  </div>
  </br>
  <div class="field">
    <%= f.label "パスワード" %>
　　<%= f.password_field :password, autocomplete: "current-password", class:"offset-md-1" %>
  </div>
  </br>

  <div class="actions">
    <%= f.submit "ログイン", class:"btn btn-info offset-md-3" %>
  </div>
<% end %>

<p>会員登録がお済でない方</p>
　　<%= render "public/shared/links" %>から新規登録を行ってください。
</div>


